enum Automatic {Waiting, Horizon, Vertical, Zone} automatic {Automatic::Waiting};

switch (automatic) {
         case Waiting:
         break;
         case Horizon:
         break;
         case Vertical:
         break;
         case Zone:
         break;
      }